version: '3'
services:
  app:
    build: .
    volumes:
      - ..:/project
      - /project/.config
      - /project/dist
      - /project/node_modules
      - /project/tmp
    env:
      - NODE_ENV=production
      - EMBER_ENV=production
      - ELECTRON_PLATFORM=linux
      - ELECTRON_ARCH=x64
